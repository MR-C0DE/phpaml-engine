# Contributing

1. Create a focused branch from `main`.
2. Keep the engine frontend-only, deterministic and accessible.
3. Add tests for every behavior or bug fix.
4. Run `composer test` and `composer validate --strict`.
5. Document public behavior changes in the README and changelog.

Do not commit secrets, dependency folders or generated local state.
